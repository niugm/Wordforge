# 07 · 功能清单与验收标准

> 验收以 **GIVEN-WHEN-THEN** 表达。每个功能给出最小可发布定义。
>
> **实现状态图例**：✅ 已实现 · 🚧 部分实现 · ⏳ 待实现

---

## F1 项目管理（多作品）✅

- ✅ 列出 / 新建 / 重命名 / 归档 / 删除作品
- ✅ 启动时记忆上次打开的作品，自动回跳 Workspace
- ✅ 编辑项目信息（简介、目标字数）
- ✅ ProjectCard 显示简介、目标字数、更新时间
- ✅ LeftSidebar "切换作品" 按钮（清状态 + 跳 Welcome）
- ⏳ 封面缩略图（依赖 plugin-fs + dialog）

**AC**
- _GIVEN_ 用户首次启动 _WHEN_ 进入应用 _THEN_ 显示 Welcome 页，引导新建作品。
- _GIVEN_ 已存在 N 个作品 _WHEN_ 启动 _THEN_ 自动打开 `last_project_id`，不存在则回到 Welcome。
- 删除作品需二次确认；级联删除其下所有章节/角色/大纲。

---

## F2 章节管理 ✅

- ✅ 树形结构（卷 → 章 → 节）任意层级
- ✅ dnd-kit 拖拽同级排序
- ✅ Alt+↑/↓ 键盘排序
- ✅ 章节状态：草稿 / 修订中 / 已完成（色点提示）
- ✅ 右键菜单：新建子章节 / 重命名 / 移动到... / 修改状态 / 上移 / 下移 / 删除
- ✅ 跨父节点移动（MoveChapterDialog）
- ✅ Rust 侧移动校验：禁止移动到自身/后代节点下，禁止跨作品挂载父章节
- ✅ 章节状态 / 重命名 / 移动后同步章节树与编辑区详情缓存
- ✅ 删除带子节点章节二次确认
- ✅ 复制章节（含子章节、正文、字数与状态）
- ✅ 导出单章节（Markdown / Plain Text，含子章节树）

**AC**
- 拖拽 100 个节点的树性能 < 16ms 帧时长。
- 删除带子节点章节需二次确认，且记入最近 30 天可恢复（通过 `revisions`）。

---

## F3 角色管理 🚧

- ✅ 角色卡 CRUD：姓名、别名、头像路径、身份、画像（Markdown）、自定义属性 JSON
- ✅ 左侧栏角色 Tab
- ✅ 列表 / 卡片视图切换
- ✅ Rust 侧校验：姓名非空，自定义属性必须是 JSON object
- ⏳ 关系：成对添加（父子、师徒等）
- ⏳ 头像文件选择与缩略图（依赖 plugin-fs + dialog）

**AC**
- 角色卡支持引用其他角色（`@姓名`）自动跳转。
- 一键"插入角色卡到当前段落"。

---

## F4 大纲管理 🚧

- ✅ 大纲节点 CRUD：标题、内容 Markdown、状态
- ✅ 树形（自由层级）
- ✅ 新建子节点 / 编辑 / 删除 / 移动到其他父节点
- ✅ 同级上移 / 下移排序
- ✅ Rust 侧移动校验：禁止移动到自身/后代节点下，禁止跨作品挂载父节点
- ⏳ 节点 ↔ 章节双向关联
- ✅ 状态：构思 / 撰写中 / 已完成

**AC**
- 大纲拖拽到章节树上自动创建对应章节。
- 章节标题改名时大纲绑定保持不变。

---

## F5 编辑器 🚧

- ✅ TipTap v3 内核
- ✅ 富文本：标题 H1-H3、加粗、斜体、下划线、删除线、引用、有序/无序列表、分隔线、代码块
- ✅ 固定工具栏（EditorToolbar）
- ✅ 选中文字浮动菜单 BubbleMenu（B/I/U/S）
- ✅ 自动保存（防抖 500ms）+ 卸载兜底保存
- ✅ 保存状态指示（已保存 / 保存中 / 失败）
- ✅ 历史撤销/重做（ProseMirror 内置）
- ✅ 专注模式：隐藏双侧栏/标题栏/页脚，保留退出浮层与沉浸式正文留白
- ⏳ 节点扩展：场景分割 / 注释 mark / TODO mark
- ⏳ 中文输入法兼容测试（compositionstart/end）
- ⏳ 任务列表

**AC**
- 输入 1 万字稳定流畅（无明显卡顿）。
- 关闭应用 → 重新打开，最近一次输入完整呈现。

---

## F6 段落精修（AI） ⏳

- ⏳ 选中段落 → 弹 popover
- ⏳ 风格：凝练 / 扩写 / 换语气 / 翻译 / 自由提示
- ⏳ 流式输出 + diff 对照
- ⏳ "应用替换" 或 "插入下方"

**AC**
- 选中范围超 5000 字时给出"段落较长，建议分段"提示。
- 网络中断时显示已生成部分，提供"继续生成"。

---

## F7 章节校审（AI） ⏳

- ⏳ 当前章节做四维（逻辑/连贯/人物口吻/伏笔）评审
- ⏳ 输出 JSON → 渲染为可点击建议列表
- ⏳ 接受 / 忽略每条建议

**AC**
- 报告生成后 `ai_messages.scope = 'review'` 留痕。
- 每条建议 "跳转" 到对应行，定位偏差 < 1 行。

---

## F8 字数与统计 🚧

- ✅ 实时字数：本章（Footer 实时显示）
- ✅ 写作会话自动开始/结束（EditorPanel mount/unmount）
- ✅ 每日字数累计写入 `writing_sessions`（localtime 时区正确）
- ✅ Dashboard：今日字数 / 本月字数 / 连续写作天数（streak）
- ✅ 30 天热力图 + Recharts 柱状图趋势
- ✅ 全书字数汇总
- ✅ 目标进度条（全书 vs target_word_count）
- ✅ 30 秒无输入自动结束 session，再次输入自动开启新 session
- ✅ Dashboard：本周字数（周一至今）
- ✅ 30 / 90 / 365 天热力图切换，趋势图刻度随范围调整
- ✅ 设置中支持字数计数模式：字符 / 不计空白 / 中英混合
- ⏳ 本段字数
- ⏳ 今日目标进度条

**AC**
- Dashboard 显示：今日/本周/本月字数、连续写作天数（streak）、目标进度、30/90/365 天热力图。
- 切换作品后看板数据按 project 隔离。

---

## F9 批注 / 标记 ⏳

- ⏳ 选中文字 → 添加 highlight / comment / TODO
- ⏳ 右侧批注面板汇总当前章节所有批注
- ⏳ 点击跳转至原位置
- ⏳ 完成 TODO 一键解决

**AC**
- 同一段同时可叠加多种标记。
- 删除被标记区域时自动清理对应 annotation 记录。

---

## F10 全文检索 🚧

- ✅ Ctrl/Cmd+Shift+F 唤起
- ✅ 基础搜索：当前作品的章节标题/摘要、角色卡、大纲节点
- ✅ 结果按类别标识，点击跳转到章节或切换左侧角色/大纲 Tab
- ✅ 前端搜索结果显示命中片段，并高亮关键词
- ✅ 章节正文 FTS5 检索（保存时写入纯文本索引）
- ⏳ 结果按作品 / 类别分组

**AC**
- 50 万字稿件查询 < 200ms（FTS5）。

---

## F11 命令面板 ✅

- ✅ Ctrl/Cmd+K 唤起（CommandPalette 框架已有）
- ✅ 接入真实章节 / 角色 / 大纲 / 命令搜索
- ✅ 章节结果可直接跳转；角色/大纲结果可切换左侧 Tab
- ✅ 最近使用置顶（持久化最近 6 个命令/章节/角色/大纲）

---

## F12 设置 🚧

- ✅ 主题切换（亮 / 暗 / 护眼）via ThemeToggle
- ✅ SettingsDialog 框架已有
- ✅ 编辑器偏好：字体族、字号、行高、编辑区宽度
- ✅ 字数计数模式配置持久化，并应用到编辑器保存字数和写作会话增量
- ✅ 备份设置：备份目录、自动备份开关、立即备份
- ✅ AI provider 配置（OpenAI 兼容 / Anthropic / Gemini、密钥、base_url、模型）

---

## F13 导出 🚧

- ✅ Markdown（每章一个 .md / 单文件合并）
- ✅ Plain Text（每章一个 .txt / 单文件合并）
- ✅ 导出完成后可从 toast 或导出页打开导出位置
- ⏳ Docx（pandoc 或 `docx-rs`）

**AC**
- 50 章作品 5 秒内完成导出。
- 元数据（作者 / 项目名 / 字数）写入 docx 属性。

---

## F14 自动备份 🚧

- ✅ 设置中保存备份目录与自动备份开关（写入 `settings` 表）
- ✅ 手动立即备份：复制当前 SQLite DB 为 `wordforge-时间戳.db`
- ✅ 应用启动后若已过本地时间 03:00，则当天最多自动备份一次
- ✅ 保留最近 7 份 `wordforge-*.db`，超出删除最旧

---

## F15 跨平台与 CI 🚧

- ✅ `.github/workflows/ci.yml`：push/PR 触发 lint、frontend build、cargo fmt、clippy
- ✅ `.github/workflows/release.yml`：tag `v*` 触发 Tauri release draft
- ✅ Windows bundle 通过 `windows-latest` 构建
- ✅ macOS universal bundle 通过 `macos-latest` 构建
- ⏳ 首次 GitHub Actions 实跑验证
- ⏳ 代码签名 / 公证 / SmartScreen 处理（v0.2）

---

## 优先级（v0.1.0 必须）

P0 = F1 ✅, F2 ✅, F5 🚧, F8 🚧, F12 🚧, F15 🚧
P1 = F3 🚧, F4 🚧, F6 ⏳, F7 ⏳, F9 ⏳, F13 🚧
P2 = F10 🚧, F11 ✅, F14 ⏳
